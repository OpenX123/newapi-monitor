{
  "name": "newapi-monitor",
  "version": "1.0.0",
  "description": "NewAPI 令牌监控面板 — 实时监控用户调用量、额度消耗、Token 使用情况，支持自动禁用超限 Token 和邮件告警",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "pg": "^8.16.0",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "node-cron": "^4.2.1",
    "nodemailer": "^8.0.3"
  }
}
